
Docker
docker run -d --rm --name mongo -p 27017:27017 -v mongodbdata:/data/db mongo
-- docker compose ile çalıştığında volume değişir

docker-compose up -d

Play.Common\src\Play.Common> Yolundan paketlemek için
dotnet pack -o ..\..\..\packages\

nuget source u eklemek
dotnet nuget add source C:\orkun\microservice-ex\packages -n PlayEconomy           

Paket
Eklemek
dotnet add package Play.Common

code . -r

dotnet add package Microsoft.Extensions.Http.Polly
Policy eklemek için mesela timeout için maksimum bekleme süresi vs. ayarlanabilir.

Play.Catalog.Service
dotnet add package MassTransit.AspNetCore
dotnet add package MassTransit.RabbitMQ